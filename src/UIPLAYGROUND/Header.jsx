import React from 'react'

const Header = ({name,child}) => {
  return (
   
   
    <a href="#" className="hover:text-orange-400 hover:underline hover:underline-offset-1">{name}</a>
  
  
  )
}

export default Header